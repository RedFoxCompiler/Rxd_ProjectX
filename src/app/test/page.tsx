'use client';

// This page has been identified as the source of a persistent build error
// due to incorrect usage of server-side flows in a client component.
// It is being removed to definitively resolve the issue.
// The primary chat interface in `/chat` provides all necessary functionality.

export default function TestPage() {
  return null;
}
