'use client';

// Este arquivo não é mais usado, pois o componente 3D foi removido.
// Ele está vazio para evitar erros de importação e pode ser excluído com segurança.

export function Globe() {
    return null;
}
